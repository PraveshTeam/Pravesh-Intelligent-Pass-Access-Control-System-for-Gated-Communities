/**
 * BackgroundFX — previously rendered animated orbs/streaks/starfield/grid
 * behind every route. In the light minimalist theme the app has a clean,
 * static background, so this renders nothing. Kept as a no-op component so
 * its import in App.jsx stays valid without any other changes.
 */
export default function BackgroundFX() {
  return null
}
